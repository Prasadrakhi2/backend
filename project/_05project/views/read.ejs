<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Document</title>
</head>
<body>
    <div class="bg-zinc-900 w-full h-full text-white p-10">

        <div class="nav mb-10">
            <a class="text-blue-400 hover:text-blue-500" href="/">back to home</a>
        </div>

        <h1 class="text-3xl tracking-tighter mb-3">All Users</h1>
            
            
            <div class="users flex flex-wrap gap-5">
                <% users.forEach(function(users){ %>
                    <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-52 rounded-lg bg-zinc-700">
                            <img src="<%= users.image%>" class="w-full h-full overflow-hidden object-cover" alt="">
                        </div>
                    <h1 class="text-xl mt-3 tracking-tighter"><%= users.name%></h1>
                    <h3 class="text-zinc-500"><%= users.email %></h3>
    
                    <div class="mt-5 flex justify-between">
                        <a class="text-green-300 hover:text-green-400 cursor-pointer" href="">EditUser</a>
                        <a class="text-red-300 hover:text-red-400 cursor-pointer" href="/delete/<%=users._id%>">DeleteUser</a>
                    </div>
                    </div>
                <%})%>
               

               
                
            </div>
           
           
           
        
    </div>
</body>
</html>